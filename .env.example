# Copy to .env and fill in once the domain is known. docker compose reads
# .env automatically; leaving these unset just means nginx-proxy and
# acme-companion sit idle and the app stays reachable on :3001 as before.

DOMAIN=
LETSENCRYPT_EMAIL=
